{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "start": "live-server --port=8080 ./",
    "build": "echo 'No build step necesario'"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
